# charts/monitoring-stack/templates/loki-lb-service.yaml
{{- if .Values.lokiLoadBalancer.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: loki-lb
  namespace: monitoring
  annotations:
    metallb.universe.tf/loadBalancerIPs: {{ .Values.lokiLoadBalancer.ip | quote }}
spec:
  type: LoadBalancer
  selector:
    app.kubernetes.io/name: loki
    app.kubernetes.io/component: single-binary   # SingleBinary Pod 셀렉터
  ports:
    - name: http
      port: 3100
      targetPort: 3100
{{- end }}